Persistent printer preference stored in JSON
for the pdf itself (!)
“Print all remaining” mode after first confirmation

---------------
at startup execute the program ebay_revenue.py

this program returns for example:
    
Total funds: 1025.96
do not report this informattion in the log window but on the status are of the program
     
add a button next to orders awaiting shipment called "Print label"
    this button runs the script "ebay_shipping.py"

add a button next to Print label called "Restock"
    this button runs the script "restock.py"
    
add a button next to Restock called "list orders awaiting shipment"
    this button runs the command "python ebay_scrape.py --headless --stdout-short"

    of course the standard output of each command is shown in the active console

add a button below these buttons "Check orders and print"
    this button will run the following command

python ebay_scrape.py --headless --stdout-short

python ebay_linker.py ^
  --orders-csv awaiting_shipment_items.csv ^
  --links-json ebay_links.json ^
  --out-links-json ebay_links.json ^
  --recursive ^
  --min-score 60 ^
  --min-margin 8 ^
  --print ^
  --printer 2 ^
  --myprint C:\Users\benoi\Downloads\ManualForge\myprint.py

you will get the printer number for this command line from the selected printer in the GUI

 next to this button there will be an option to :
    always ask printer 
in that caser pass the argument to the command line
    --always-ask-printer
    instead of 
  --printer 2